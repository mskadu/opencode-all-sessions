{
  "name": "opencode-all-sessions",
  "version": "0.1.0",
  "description": "OpenCode TUI plugin — list and jump to sessions across all projects from anywhere",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "@opencode-ai/plugin": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
