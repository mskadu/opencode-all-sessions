name: CI
on:
  push:
    branches: [main]
    paths:
      - src/**/*.ts
      - tsconfig.json
      - vitest.config.ts
      - package.json
      - package-lock.json
  pull_request:
    branches: [main]
    paths:
      - src/**/*.ts
      - tsconfig.json
      - vitest.config.ts
      - package.json
      - package-lock.json
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
