name: CI
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/ci.yml
      - src/**/*.ts
      - tsconfig.json
      - vitest.config.ts
      - package.json
      - package-lock.json
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/ci.yml
      - src/**/*.ts
      - tsconfig.json
      - vitest.config.ts
      - package.json
      - package-lock.json
jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
